{% extends 'base.html' %}

{% block content %}
<a href="/agendas/add" class="btn btn-primary">Nova Agenda</a>
<br>
<table class="table table-condensed" border="1" width="30%">
  <tr>
    <th>Nome</th>
    <th>Telefone</th>
    <th>Email</th>
    <th>Ação</th>
  </tr>
  {% for agenda in agendas %}
  <tr>
    <td>{{ agenda.get('nome') }}</td>
    <td>{{ agenda.get('telefone') }}</td>
    <td>{{ agenda.get('email') }}</td>
    <td><a href="/agendas/{{ agenda._id }}/edit/">Editar item</a></td>
    <td><a href="/agendas/{{ agenda._id }}/delete/">Excluir item</a></td>
  </tr>
  {% endfor %}
</table>
{% endblock %}
