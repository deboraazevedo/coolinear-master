{% extends 'base.html' %}

{% block content %}
  <h2> Histórico de comandos de  {{ usuario.nome_completo }} ({{ usuario.nome_usuario }})</h2>

  <table class="table table-condensed" border="1" width="30%">
  <tr>
    <th>Usuário</th>
    <th>Data</th>
    <th>Hora</th>
    <th>Comando</th>
  </tr>
  	{% for usuario in usuarios %}
  	  {% for history in histories %}
  	    <tr>
          <td><a href = "/usuarios/{{ usuario.nome_usuario }}/ "> {{ usuario.nome_usuario }} </a> </td>
          <td><a href = "{{ history.data.year }}/ "> {{	history.data.year }} </a>/ {{	history.data.month }} / {{	history.data.day }}</td>
          <td>{{	history.data.hour }} : {{	history.data.minute }} : {{	history.data.second }}</td>
          <td>{{ history.comando }}</td>
        </tr>
  	  {% endfor %}
    {% endfor %}
  </table>

{% endblock %}
